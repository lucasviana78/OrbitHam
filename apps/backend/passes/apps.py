"""App configuration for the passes module."""
from django.apps import AppConfig


class PassesConfig(AppConfig):
    """Configuration for the passes Django app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "passes"
