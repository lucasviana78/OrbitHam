"""App configuration for the satellites module."""
from django.apps import AppConfig


class SatellitesConfig(AppConfig):
    """Configuration for the satellites Django app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "satellites"
